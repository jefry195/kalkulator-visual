# Project Rules

## Port & Server Management
- **Never use or hijack ports that are already in use** by existing applications or background services.
- Always check port availability or use unused/dynamic ports when launching new local dev servers or services.
- Never guess or probe arbitrary ports that might conflict with the user's active applications.
